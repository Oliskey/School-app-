# API Reference

Document each service's public endpoints in its service folder under `services/<service>/API.md`.

Example:
- `GET /api/schools` — list schools
- `POST /api/auth/login` — user login

Keep detailed request/response shapes in `shared/types`.
