# Example environment variables (no secrets in repo)
NODE_ENV=development
PORT=3000
DATABASE_URL=postgres://user:password@localhost:5432/school_app_db
# Do NOT store real secrets here. Use your secret manager.
