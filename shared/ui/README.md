Shared UI components live here. Teams:
- `@frontend` owns `apps/web-client`
- Keep components framework-agnostic if possible.

Publish strategy:
- Use a private npm package or path-based imports between workspaces.
